# ✈️ SyncTrip 백엔드 구현 현황 및 보완 로드맵 (2026-05-20 기준)

본 문서는 현재 `SyncTrip-Spring` 프로젝트의 백엔드 구현 상태를 분석하고, 인수인계 문서(v6) 및 서비스 개요 대비 미흡한 점을 정리한 보고서입니다.

---

## 📊 1. 전체 현황 요약

| 분류 | 상태 | 주요 내용 |
| --- | --- | --- |
| **핵심 도메인** | ✅ 구현 완료 | 밴드(그룹), 장소(Place), 장바구니(Pick), 투표(Vote), 일정(Schedule) |
| **알고리즘** | ⚠️ 보완 필요 | Step 1~3 파이프라인은 동작하나, 영업시간/숙소 연동/부분 재계산 미흡 |
| **부가 기능** | ❌ 미구현 | 가계부/정산(OCR), 공유 앨범, 여권 스탬프 |
| **인프라** | ✅ 구현 완료 | WebSocket(실시간 투표/상태), Spring Security, Social Login |

---

## ✅ 2. 구현 완료 항목 (Implemented)

현재 안정적으로 동작하며 프론트엔드와 연동 가능한 기능들입니다.

### 2.1 밴드 및 협업 환경
- **그룹 관리**: 밴드 생성, 초대 코드 발급/가입, 방장(OWNER) 권한 관리.
- **Ready 시스템**: 전원 준비 완료 시 자동 투표 단계(VOTING) 진입. (취소 불가 로직 포함)
- **실시간 통신**: WebSocket(STOMP)을 통해 투표 현황, 준비 상태, 단계 전이 실시간 브로드캐스팅.

### 2.2 장소 및 투표
- **개인별 장바구니**: 사용자당 최대 5개 장소 Pick 가능. (동시성 제어를 위한 PESSIMISTIC_WRITE 적용)
- **스와이프 투표**: LIKE/DISLIKE 투표 처리. 본인이 담은 장소는 자동으로 LIKE(result=0) 처리.

### 2.3 알고리즘 파이프라인 (기본형)
- **Step 1 (Weighted Cost)**: 투표율과 거리를 기반으로 우선순위 점수(Priority Score) 계산 및 mainPool/altPool 분리.
- **Step 2 (K-Means)**: 여행 일수(K)에 맞춰 지리적 클러스터링 및 장소 배분.
- **Step 3 (Simple TSP)**: Nearest Neighbor 알고리즘을 통한 일차별 동선 최적화 및 시간 할당.

---

## ⚠️ 3. 보완 필요 항목 (Needs Improvement)

인수인계 문서 v6의 명세와 다르거나, 기술적 완성도가 낮은 부분입니다.

### 3.1 영업시간(Opening Hours) 처리의 단순화
- **문제점**: 현재 `ScheduleService`는 장소의 여러 영업시간 중 '첫 번째' 것만 가져와 모든 요일에 동일하게 적용합니다.
- **이유**: `SimpleTsp` 알고리즘이 특정 요일(MON, TUE...)에 따른 멀티 시간대 로직을 갖추지 못함.
- **영향**: 월요일 휴무인 장소가 월요일 일정에 포함되는 등 부적절한 초안 생성 가능성.
- **보완**: `OpeningHours`를 요일별 Map 형태로 확장하고, `SimpleTsp`에서 `day` 정보를 기반으로 요일별 검증 로직 추가 필요.

### 3.2 숙소(Accommodation) 기반 동선 부재
- **문제점**: 일정의 시작점(Start Node)이 '최고 우선순위 장소'로 고정되어 있습니다.
- **이유**: `AlgorithmInput` 및 `Step3Input`에 숙소 좌표가 전달되지 않으며, TSP 시작점이 숙소로 설정되지 않음.
- **영향**: 숙소에서 멀리 떨어진 곳부터 일정이 시작되어 동선 낭비 발생.
- **보완**: `GroupInfo`에 숙소 좌표를 추가하고, `SimpleTsp`의 첫 번째 노드를 숙소로 강제 지정해야 함.

### 3.3 부분 재계산(Partial Recalculation) 미구현
- **문제점**: 여행 중 숙소를 변경하거나 일정을 수정할 때 전체 일정을 삭제하고 다시 생성합니다.
- **이유**: `ScheduleService.generateInternal`이 항상 `deleteAll` 후 `saveAll`을 수행함.
- **영향**: 사용자가 과거 일차에 수동으로 수정한 내역이 모두 날아감.
- **보완**: `current_date` 파라미터를 도입하여, 해당 날짜 이후의 일정만 삭제/재생성하는 로직(FIX-36 준수) 구현 필요.

### 3.4 Plan B 추천 반경 및 단계 불일치
- **문제점**: 코드상 1, 2, 3km의 3단계로 구현되어 있으나, 문서상으로는 1km 이내 2단계 폭포수 검색을 권장함.
- **보완**: 도보 여행 위주인 서비스 특성에 맞춰 검색 반경을 재조정하고, '같은 카테고리' 필터링을 강화해야 함.

---

## ❌ 4. 미구현 항목 (Unimplemented)

졸업 프로젝트 및 서비스 완성도를 위해 반드시 추가되어야 할 핵심 기능들입니다.

### 4.1 가계부 및 정산 시스템 (Finance)
- **누락 내용**: `expenses`, `expense_members` 테이블은 설계되어 있으나 관련 Service/Controller가 전무함.
- **핵심 기술**: 영수증 OCR(Vision API) 연동 및 최소 환승 횟수 기반 정산 알고리즘.
- **우선순위**: **최상** (여행 중/후 핵심 가치).

### 4.2 공유 앨범 및 아카이빙
- **누락 내용**: 밴드원들이 사진을 올리고 공유하는 기능.
- **기술적 제언**: AWS S3 또는 Firebase Storage 연동 필요.

### 4.3 여권 스탬프 (Gamification)
- **누락 내용**: 여행 완료 시 특정 조건에 따른 칭호 부여 및 스탬프 적립.
- **기술적 제언**: `BandStatus.DONE` 전이 시 통계 데이터를 집계하는 배치/비동기 로직 필요.

---

## 🚀 5. 향후 개발 로드맵 제안

1.  **Phase 1 (알고리즘 고도화)**:
    - 숙소 좌표 연동 및 TSP 시작점 수정.
    - 요일별 영업시간 데이터 정규화 및 `SimpleTsp` 검증 로직 반영.
    - `current_date` 기반 부분 재계산 로직 추가.

2.  **Phase 2 (핵심 부가기능 구현)**:
    - 가계부(Expense) 기본 CRUD 및 정산 API 개발.
    - 외부 Vision API(Google/Gemini) 연동을 통한 영수증 스캔 기능.

3.  **Phase 3 (사용자 경험 개선)**:
    - 공유 앨범 기능 추가.
    - 여행 종료 후 통계 및 스탬프 시스템 구현.

---
**작성자**: Gemini CLI (Senior Backend Analyst)  
**참조**: SyncTrip_인수인계문서_v6.md, SyncTrip_서비스개요.md
