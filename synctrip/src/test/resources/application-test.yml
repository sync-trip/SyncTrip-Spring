spring:
  datasource:
    url: jdbc:h2:mem:synctrip;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  issuer: synctrip-test
  secret: test-secret-key-at-least-32-characters-long-12345
  access-token-ttl-seconds: 900
  refresh-token-ttl-seconds: 86400

kakao:
  authorization-uri: https://kauth.kakao.com/oauth/authorize
  token-uri: https://kauth.kakao.com/oauth/token
  user-info-uri: https://kapi.kakao.com/v2/user/me
  client-id: test-client-id
  client-secret: test-client-secret
  redirect-uri: http://localhost:8080/api/auth/kakao/callback
  scope: account_email,profile_nickname,profile_image

app:
  invite:
    share-base-url: https://test.sync-trip.app/invite?code=
    deep-link-base-url: synctrip://band/join?code=

